# Secrets — NEVER commit
.env
*.env

# Python
__pycache__/
*.pyc
venv/
.venv/

# NOTE: jobs.db is intentionally NOT ignored — the GitHub Actions workflow
# commits it back between runs so the `posted` flag persists (prevents
# re-posting the same jobs each cron run). See .github/workflows/jobs.yml.
